# 三项修改综合审查

> 审查日期：2026-06-15
> 审查范围：`收藏功能优化-段落收藏气泡.md`、`收藏功能Phase3技术实现.md` / `收藏功能Phase3实现审查.md`、`技术社区AI坏链接修复说明.md` 及其对应代码
> 结论：前次审查发现的 P2/P3 已落实；本轮复审未发现新的代码阻断问题。仍需真实浏览器 / GitHub Pages / Obsidian 端到端人工验收。

## 1. 历史 Findings（已复核）

### P2-1 `pnpm format:check` 失败，坏链接测试文件未格式化

**复审状态：已修复。** `src/__tests__/generate-manifest.test.ts` 已格式化，本轮 `pnpm format:check` 通过。

**位置**

- `src/__tests__/generate-manifest.test.ts`

**证据**

```text
pnpm format:check
[warn] src/__tests__/generate-manifest.test.ts
[warn] Code style issues found in the above file.
```

这属于“技术社区 AI 坏链接修复”对应测试文件的问题。`pnpm test` 已经通过，但如果 CI 或提交前检查包含 `format:check`，当前工作区仍不能算绿。

**建议**

运行 Prettier 修复该文件，或手动按 Prettier 格式调整。修完后重跑 `pnpm format:check`。

### P2-2 段落收藏气泡仍会在鼠标离开气泡时隐藏，未完全满足“选区还在就一直显示”

**复审状态：已修复。** `showPopover(..., sticky=false)` 已加入 sticky 模式，段落选择气泡传 `sticky=true`，`mouseleave` 对 sticky 气泡不再隐藏；链接 hover 气泡仍保持原自动隐藏行为。

**位置**

- `index.html:1146-1158`：共享 popover 仍保留 `mouseleave -> setTimeout(hidePopover, 140)`。
- `index.html:1219-1260`：段落选择气泡改为 selection-driven hide，但复用了同一个 popover。
- `收藏功能优化-段落收藏气泡.md:36-42`：文档目标是“只要划选高亮还在，气泡就一直停留；滚动与移动鼠标都不再隐藏它”。

**问题**

这次确实删除了 selection path 上的 scroll/mousedown 脆弱隐藏，并用 `selectionchange` 判断选区是否折叠。但 popover 是链接 hover 与段落选择共享的，`ensurePopover()` 里的 `mouseleave` 仍然会无条件 `hidePopover()`。

因此复现场景仍存在：

1. 划选一段文字，气泡出现。
2. 鼠标移到气泡上但未点击。
3. 鼠标稍微移出气泡。
4. 即使选区仍然高亮，气泡也会在约 140ms 后消失。

这和“只要选区还在按钮就一直显示”的验收目标不一致。

**建议**

- 给 `showPopover` 增加模式，例如 `{ stickyWhileSelection: true }`。
- 链接 hover 继续使用 `mouseleave` 隐藏。
- 段落选择气泡在 `mouseleave` 时不隐藏，只由 `selectionchange` 折叠、点击收藏完成、或显式清选区来隐藏。
- 补一个轻量 DOM/Playwright 手测用例：选区存在时 mouseenter/mouseleave 气泡不消失。

### P3-3 坏链接说明文档的验证结果已过期

**复审状态：已修复。** `技术社区AI坏链接修复说明.md` 已更新为当前事实：全量 `pnpm test` 16 files / 243 tests passed，旧的 favorites/sync 失败记录已删除或改正。

**位置**

- `技术社区AI坏链接修复说明.md:159-182`

**问题**

文档仍写着全量 `pnpm test` 有 10 个收藏相关失败。当前复审实际结果是：

```text
pnpm test
16 files passed
243 tests passed
```

现在真实遗留问题不是测试失败，而是 `pnpm format:check` 因 `src/__tests__/generate-manifest.test.ts` 未格式化失败。

**建议**

更新说明文档的验证结果，避免下一个 Agent 误以为当前还有收藏测试红灯，或者忽略真正的格式红灯。

### P3-4 坏链接修复缺少前端入口的直接回归测试

**复审状态：低风险可接受。** 当前 `generate-manifest.ts` 有回归测试；本轮额外抽取 `index.html` 内联函数验证 Dev.to 样例输出一致。后续可补前端抽取测试，但不构成提交阻断。

**位置**

- `index.html:451-466`
- `src/generate-manifest.ts:82-93`
- `src/__tests__/generate-manifest.test.ts:79-88`

**问题**

`generate-manifest.ts` 的 `normalizeBrokenMarkdownLinks()` 有测试，`index.html` 里复制了一份同名逻辑并在 `renderMarkdown()` 前调用，但没有测试直接抽取前端版本。当前人工抽取验证通过，页面端与 RSS 端输出一致；不过复制实现后续容易漂移。

**建议**

可以接受为低风险；若继续加固，建议新增一个 `index.html` 函数抽取测试，至少覆盖用户提供的 Dev.to 坏链接样例，保证页面端不漂移。

## 2. 已确认正常的部分

- Phase 3 第三轮 UTF-8 tie-break 已落地：`index.html` 使用 `TextEncoder` 按 UTF-8 字节比较，Go 侧字符串比较保持 UTF-8 字节序；JS/Go 都补了 `["😀"]` vs `["￿"]` 的非 ASCII tie 测试。
- Phase 3 搜索、日期、标签 AND、`edited_at` 同步水位、标签/备注 LWW、Unicode 截断等现有测试通过。
- 技术社区 AI 坏链接的核心样例在 `index.html` 抽取验证中输出正确 Dev.to URL；`generate-manifest.ts` 的回归测试也覆盖该样例。
- 段落气泡的主改动方向正确：气泡出现不再等 sha256；短文本阈值从 8 降到 2；scroll/mousedown 不再直接打掉 selection popover。

## 3. 上轮验证结果

已执行：

```text
pnpm typecheck                              通过
pnpm lint                                   通过
pnpm test                                   16 files / 243 tests passed
node index.html inline script syntax check  通过
index.html Dev.to bad-link normalization    通过
git diff --check                            通过
cd bridge && gofmt -d                       无输出
cd bridge && go vet ./...                   通过
cd bridge && go build ./cmd/radar-bridge    通过
cd bridge && go test -race -cover ./...     coverage 45.5%
```

未通过：

```text
pnpm format:check
失败文件：src/__tests__/generate-manifest.test.ts
```

未执行真实浏览器端到端；仍建议人工验收：

- 段落划选后气泡停留、点击收藏、取消收藏、滚动不消失。
- 链接 hover 收藏气泡未回归。
- GitHub Pages 上 `2026-06-14 / ai-community` Dev.to 链接可打开。
- Feed 中同一坏链接已规范化。
- Phase 3 收藏视图搜索/筛选/展开/编辑/同步双产物。

## 4. 最终复审结果（2026-06-16）

本轮针对三组修改再次复核：

- `收藏功能优化-段落收藏气泡.md` 与 `index.html`：sticky popover 已落实，段落选择气泡不会再因 mouseleave 自动隐藏；链接 hover 气泡仍非 sticky。
- `收藏功能Phase3技术实现.md` / `收藏功能Phase3实现审查.md` 与代码：UTF-8 byte-order tie-break 已在 JS/Go 双端落地，非 ASCII 用例已覆盖。
- `技术社区AI坏链接修复说明.md` 与 `src/generate-manifest.ts` / `index.html`：Dev.to 坏链接样例在 feed 生成路径和页面渲染路径均能规范化为正确 URL。

本轮验证：

```text
pnpm format:check                          通过
pnpm typecheck                             通过
pnpm lint                                  通过
pnpm test                                  16 files / 243 tests passed
node index.html inline script syntax check 通过
index.html Dev.to bad-link normalization   通过
cd bridge && gofmt -d                      无输出
cd bridge && go vet ./...                  通过
cd bridge && go build ./cmd/radar-bridge   通过
cd bridge && go test -race -cover ./...    coverage 45.5%
```

未执行真实端到端；提交后仍需人工检查 GitHub Pages 页面、段落气泡鼠标交互、Bridge 同步到 GitHub + Obsidian 双产物。
