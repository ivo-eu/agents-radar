# Bug 文档:对话中工具调用反复"卡住"

> 说明:本文中凡是展示标签语法处,一律用全角括号「【】」或占位符代替真实尖括号，
> 以免文档内容本身被运行时再次误当成一次工具调用。

## 元信息

| 项 | 值 |
|---|---|
| 会话 ID (session) | `0c5808a2-3a9d-4317-9879-6780711a3da5` |
| 会话记录文件 | `~/.claude/projects/-Users-dinsafer-Desktop-ai---/0c5808a2-3a9d-4317-9879-6780711a3da5.jsonl` |
| 项目路径 | `/Users/dinsafer/Desktop/ai学习区/agents-radar` |
| 发生日期 | 2026-06-16 |
| 环境 | Claude Code(VSCode 扩展),模型 claude-opus-4-8,macOS(Darwin 22.6.0),zsh |
| 现象关键词 | 用户多次反馈"卡住了 / 又卡住了 / 每次出现 invoke 结束标签就卡住" |

---

## 一句话结论

**不是真的卡死(无死循环、无超时挂起),而是助手在某些轮把工具调用写成了"错误的标签语法"——缺少 `antml:` 命名空间前缀的 invoke 标签。这种格式不能被运行时解析为一次有效工具调用,于是那一轮"什么都没执行",在用户视角看就像"卡住了"。**

用户准确抓到了规律:**凡是那一轮以裸的 invoke 结束标签(无前缀)收尾,就卡。** 因为那正是格式写错的信号。

---

## 正确 vs 错误的标签格式

运行时要求工具调用使用带命名空间前缀的标签。用占位符表示:

- 正确(可解析、工具会真正执行):
  - 开始标签:`【a:invoke name="Bash"】`(即带 `antml:` 前缀)
  - 参数:`【parameter name="command"】...【/parameter】`
  - 结束标签:`【/a:invoke】`(带前缀)

- 错误(无法解析 → 该轮无任何工具执行 → 看似"卡住"):
  - 开始标签写成了不带前缀的 `【invoke name="Bash"】`
  - 结束标签写成了不带前缀的 `【/invoke】`

关键点:**结束标签是否带前缀,是这次 bug 最显眼的外部特征**。带前缀的那些调用全部成功;不带前缀的那些全部"卡住"。

---

## 复现/触发条件(本会话观测到的规律)

1. 同一条助手消息里,**正文叙述之后紧跟的第一个工具调用**更容易写错前缀(注意力从"写中文解释"切换到"写工具调用"时,前缀漏写)。
2. 一旦某一轮写成无前缀格式,该轮就不会有任何工具结果返回;用户看到的是"停住/卡住"。
3. 用一个**简单的探针调用先破冰**(例如先发一个 `echo` 的 Bash),再发后续真正的调用,能显著降低写错概率——因为破冰调用强制进入了"正确工具调用"的格式上下文。这一招在本会话中被反复用到并有效。

---

## 根因分析

- **直接原因**:助手输出的工具调用 XML 标签缺少 `antml:` 命名空间前缀,导致 harness 的工具调用解析器不识别,既不报"参数错误"也不执行,整轮静默失败。
- **诱因**:
  - 在中英文混排、长正文之后切换到工具调用时,容易把带前缀的标签写成裸标签。
  - 在文档/示例里书写"真实标签字符"时,如果不转义,文档内容本身也可能被解析器二次误读(本会话写这份文档时就发生过一次,见下"附:写文档时的二次踩坑")。
- **非原因(已排除)**:
  - 不是网络超时(中途确有一次 `gh run watch` 因 `unexpected EOF` 中断,但那是 GitHub API 偶发,与"卡住"现象是两回事)。
  - 不是命令本身执行慢或死循环(成功执行的同类命令都在秒级返回)。
  - 不是权限被拒(被拒会明确返回 rejection,与静默无返回不同)。

---

## 影响

- 仅影响"交互流畅度":每次写错都要用户提示"卡住了",助手重发一次才继续。
- **不影响最终正确性**:所有实际改动(任务 A/B/C、提交、push、CI 验证)都在重发为正确格式后成功完成,没有任何代码因此出错或丢失。

---

## 解决办法 / 规避措施

1. **始终使用带 `antml:` 前缀的 invoke 标签**(开始与结束标签都要带)。这是唯一会被运行时接受的格式。
2. **正文之后的第一个工具调用尤其要检查前缀**;必要时先发一个无害探针调用破冰。
3. **写含标签示例的文档时,把尖括号转义成占位符**(本文件即采用全角括号),避免文档被当成工具调用。
4. 若再次出现"卡住":先怀疑"上一轮是不是写成了无前缀标签",直接以正确格式重发同一个调用即可,无需改动其它内容。

---

## 附:写这份文档时的二次踩坑

第一次写本文件时,正文里放了**真实的标签字符**作为"正确示例"。结果该示例被运行时当成了一次真实(但参数不全)的 Bash 工具调用,返回了
`InputValidationError: ... required parameter \`command\` is missing`。

这反过来印证了根因:**运行时是按标签字符来识别工具调用的,文档里出现真实标签字符也会被误解析**。修正方式就是把文档中的标签一律改为占位符/全角括号(即当前版本)。

---

## 时间线(本会话)

1. 任务进行中,多次出现"卡住"——对应那几轮工具调用标签缺前缀。
2. 用户多次反馈,并最终点明"每次出现 invoke 结束标签就卡住"。
3. 助手定位为标签前缀问题,改用"探针破冰 + 全程带前缀"的方式,后续不再卡。
4. 三个业务任务(工作区清理 / 日报定时修复 / 周报月报独立)均已完成、提交、push 并通过 CI 验证。
5. 应用户要求,产出本 bug 文档。
