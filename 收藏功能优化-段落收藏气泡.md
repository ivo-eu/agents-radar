# Radar 收藏功能优化 · 段落收藏气泡（出现与停留）

> 日期：2026-06-15
> 范围：日报页面「划选文字 → 收藏段落」气泡按钮的交互体验。
> 实现位置：`index.html` 的 `setupSelectionFavorites()`（1228-1272 行）+ popover 基础函数扩展（1156-1177 行）。
> 状态：**已实现**（前端单文件；内联脚本 `node --check`/`pnpm format:check` 全过）。属于收藏功能上线后的体验打磨，不新增数据字段、不改 Bridge。

---

## 1. 背景与问题

收藏段落的交互是：在日报正文里用鼠标划选一段文字，光标右上方浮出「收藏段落」气泡按钮，点击即收藏并高亮所在块。上线后这个气泡很难用：

- **闪一下就消失**：按钮出现后，鼠标/触控板稍微一动、或页面轻微滚动，按钮立刻没了。
- **有时一开始就不出现**：划选短句/术语时不弹；划选完操作稍慢时也可能不弹。

用户确认的优化方向：**优先电脑端（鼠标划选）**、**只要选区还在按钮就一直显示**。本次不引入触屏支持。

## 2. 根因（优化前代码）

逻辑集中在 `setupSelectionFavorites()`，三处缺陷叠加：

1. **`scroll` 捕获监听把按钮立刻干掉** — `document.addEventListener('scroll', hidePopover, true)`。
   捕获模式（第三参 `true`）会捕获页面上**任意元素**的滚动事件，划选时的轻微滚动、惯性滚动、触控板微小滚动都会触发 `hidePopover`。这是「闪一下就消失」的主因。

2. **`mousedown` 一律隐藏** — 只要在 popover 外按下就 `hidePopover`，没有「选区仍在则保留」的判断，配合 scroll 隐藏让按钮极其脆弱。

3. **字符门槛偏高 + 异步链拖慢出现** —
   - `selectedText.length < 8` 直接 `return`，短句/术语/英文词组划不出按钮。
   - `mouseup` 后 `setTimeout(10ms)` 再 `await favId()` + `await blockAnchorId()`（两次 sha256）才显示，按钮姗姗来迟；这两个 hash 只有点击收藏时才真正需要，提前算既拖慢出现、又在选区被清时白算。

## 3. 优化方案（选区驱动显隐）

核心思路：**让气泡的生命周期跟随「选区是否仍然有效」**，而不是跟随脆弱的滚动/按下事件。改动全部在 `setupSelectionFavorites()` 内。

### 3.1 修「闪一下就消失」（核心）

- 删除 `scroll`（捕获）隐藏监听与 `mousedown` 全隐藏监听。
- 改为 **`selectionchange` 选区驱动隐藏**：仅当 `window.getSelection().isCollapsed`（选区被清空，即用户点了别处 / 开始新划选）时才 `hidePopover()`。
- 加 **50ms debounce**，忽略划选拖拽过程中可能出现的瞬时折叠中间态，避免误隐藏。

效果：**只要划选高亮还在，气泡就一直停留**；滚动与移动鼠标都不再隐藏它；点击空白处或重新划选清掉选区时气泡才消失。

#### 补修：共享 popover 的 `mouseleave` 仍会隐藏（综合审查 P2-2）

段落气泡复用了链接 hover 共享的 popover，而 `ensurePopover()` 里的 `mouseleave` 会无条件 `setTimeout(hidePopover, 140)`。所以即便选区还在，「鼠标移到气泡上又移出」仍会让气泡在 ~140ms 后消失，未完全满足验收目标。修复：

- `showPopover(x, y, text, action, sticky=false)` 新增 `sticky` 参数，记到 `popover._sticky`。
- `mouseleave` 隐藏前判断 `if (popover._sticky) return`——sticky 气泡交给 `selectionchange` / 点击收藏 / 清选区来隐藏，移出不再隐藏。
- 段落气泡调用处传 `sticky=true`；**链接 hover 气泡不传（默认 false），保持原有自动隐藏**，无回归。
- `hidePopover()` 同时清 `popover._sticky = false`，避免标志残留影响下一次链接 hover。

### 3.2 修「短句不出现」

- 字符门槛 `selectedText.length < 8` → `< 2`，放行短术语/词组（仍过滤空选与误触单字符）。

### 3.3 修「出现慢 / 偶尔不出现」

- `mouseup` 同步读取 `selectedText` 与 `block` 后**立即 `showPopover`**，气泡文案固定为「收藏段落」。
- 把 `favId` / `blockAnchorId` 两次 sha256 **延后到气泡的点击回调内**（用 `async` 回调）——它们只有保存那一刻才需要。气泡出现因此是瞬时的。

> 说明：因为 id 在点击前未知，气泡文案统一显示「收藏段落」。对划选新文本（绝大多数场景）文案准确；对极少数「重复划选已收藏段落」的情况，点击仍会经 `toggleFavorite` 正确取消收藏，仅气泡这一刻的文案显示为「收藏」——可接受。

## 4. 已改动项 / 未改动项

**已改动**（popover 基础设施最小扩展）：
- `showPopover(x, y, text, action, sticky=false)`：新增第 5 参 `sticky`，段落气泡传 `true`。
- `ensurePopover()`：`mouseleave` 时加 `if (popover._sticky) return`，sticky 气泡不由 mouseleave 隐藏。
- `hidePopover()`：同时清 `popover._sticky = false`，防止标志残留。

**未改动**（无回归保证）：
- CSS `.fav-popover` 样式、`mousedown` 时 `preventDefault` 保选区的行为不变。
- 链接 hover 的「收藏链接」气泡（`bindLinkHover`）：调用 `showPopover()` 时不传 `sticky`（默认 false），保持原有 mouseleave 自动隐藏。
- 不新增数据字段、不改同步 / Bridge / 路由 / 安全配对。

## 5. 验证

- 静态：`index.html` 内联脚本 `node --check` 通过（lint/typecheck 仅覆盖 `src/`，不含 `index.html`）。
- 端到端（建议本地 `npx serve .` 后在浏览器执行）：
  1. **出现稳定**：划选一段 → 气泡立即出现；划选 2-3 个字的短词 → 仍出现。
  2. **停留稳定**：气泡出现后滚一下滚轮、移动鼠标 → 气泡**不消失**（本次核心修复点）。
  3. **正确消失**：点击空白处 / 开始新划选 → 旧气泡消失。
  4. **功能正确**：点「收藏段落」→ 段落高亮 + 左侧「我的收藏 / 段落」新增一条；再次划选同段落点击 → 正确取消收藏。
  5. **回归**：链接 hover 的「收藏链接」气泡仍正常。
