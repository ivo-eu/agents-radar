# 日报定时爬取失败 — 诊断与交接文档

> 给接手的人/AI:本文记录"日报定时爬取一直不触发"的完整排查过程、已试过的方法、
> 当前结论,以及**下一步该怎么做**。
>
> ⚠️ **请先读最顶部的「最终结论(2026-06-22 实测,覆盖旧结论)」一节。** 下方
> 2026-06-18 写的「当前结论(fork 事件被丢弃)」**已被实测推翻,仅作排查过程留存**。

## 最终结论(2026-06-22 实测,覆盖 06-18 旧结论)

经过 06-18 ~ 06-22 连续 4 天实跑,真因和 06-18 当天的推断**不一样**。把话说准:

### 真因:GitHub 把低优先级仓库的 scheduled 任务统一推迟约 11–13 小时(不是不触发)

调度器 `ivo-eu/agents-radar-cron`(独立 public 仓)**每天都成功触发了**,日报 06-19 / 06-20 / 06-21 **一天没落**。问题不是"不跑",而是"跑得很晚":

| 日期 | cron 设定(UTC) | 实际触发(UTC) | 延迟 | 北京时间 |
|---|---|---|---|---|
| 06-19 | 23:17 | 12:57 | ~13.7h | 当日 20:57 |
| 06-20 | 23:17 | 10:17 | ~11.0h | 当日 18:17 |
| 06-21 | 23:17 | 11:25 | ~12.1h | 当日 19:25 |

对照上游 `duanyytop/agents-radar`(成熟、839 星、独立):cron `0 0` → 实际 00:3x UTC,**延迟仅 ~30 分钟**。

### 三条已被数据焊死的结论

1. **不是"fork 不触发"。** 06-18 写的"fork 事件被静默丢弃"是错的。独立非 fork 的调度器**照样**被延迟 11–13 小时 → 拖慢的是**低优先级/低声誉仓**(0 星、新建),不是"fork"这个身份。fork 主仓和独立调度器在同一账号 `ivo-eu` 下表现一致。
2. **改 cron 时间无效。** 延迟窗口是 GitHub 固定的(~10–13 UTC),与 cron 写几点无关:fork 早期 cron `0 0`、调度器 cron `23:17`,两个不同时间**都落在同一窗口**。且上游与 fork 早期用**同一个 `0 0`**,延迟却是 30 分钟 vs 10 小时 —— 证明决定延迟的是"哪个仓",不是"几点"。cron 也**没有时区**,永远按 UTC。
3. **detach fork 大概率救不了准点。** 独立调度器已经证明:非 fork 的新仓一样被延迟。脱离 fork 能不能把延迟降到上游那样的 30 分钟,**无把握**(可能跟仓库声誉/账号有关,无法仅凭本账号数据证实)。

### 当前状态与决定(用户 2026-06-22 拍板)

- 调度器已稳定运行,**每天自动出报,落在北京傍晚约 18–21 点**。用户决定:**接受傍晚出报,当晚报读,不再改动。**
- 今天起无需任何维护;06-22 当晚那个窗口会自动出报,无需补跑。
- 唯一脆弱点:调度器仓 secret `DISPATCH_TOKEN` 是本机 gh 的大权限 OAuth token。**若该 token 过期/重置,调度器会静默失效(日报不再出)** → 届时发现晚上无日报,优先查 token。

### 若将来又想要"早上准点"

GitHub cron 这条路准点无望(已证)。唯一可靠的准点方案是**外部 serverless 定时器**(如 cron-job.org,免费、24/7、不依赖本机)定点打 `POST .../weekly-digest.yml/dispatches`。**若走这条,务必新建仅 `actions:write`、仅限 agents-radar 的 fine-grained PAT**,不要把大权限 token 填进第三方。

---

## 背景

- 仓库:`ivo-eu/agents-radar`,是 `duanyytop/agents-radar` 的 **fork**。
- 功能:GitHub Actions 定时跑脚本,生成 AI 情报日报/周报/月报,提交回仓库,GitHub Pages 展示。
- 症状(用户长期反馈):**日报定时爬取一直失败,改了好多次都没好;周报"看起来"正常。**

## 排查时间线与已试方法

1. **最初假设:cron 写错 / 排队延迟。** 否定。日报 cron 写法正确;触发时间漂移 10+ 小时,不是排队能解释的。

2. **第二假设:改名丢调度。** 部分成立但不是全部。
   - 事实:原 `daily-digest.yml` 在 2026-06-15 被改名为 `daily-morning-digest.yml`(commit `2890c1a`,信息写 "re-register morning digest schedule")。
   - GitHub 机制:改 workflow **文件名** = 旧 workflow 注销、新 workflow 注册。
   - 实测:改名后的新 daily workflow(id `296046620`,created 2026-06-15)的 `event=schedule` 触发历史 = **0 次**(只有手动 workflow_dispatch)。
   - 对照:周报/月报 workflow(id `291903671`/`291903670`,created 2026-06-09)文件名三个月没动。
   - **采取的修复:** 固定文件名不再改;统一 cron 为无歧义错峰(日报 23:17 UTC、周报周日 22:00 UTC、月报每月1日 00:30 UTC)。push 后**仍未触发**。→ 说明"改名"不是唯一原因。

3. **第三假设(当前结论):fork 仓库的 scheduled workflow 整体不可靠。** 见下。
   - **采取的修复:** 以"看起来能触发的周报老 workflow"为准,把日报定时**并入** `weekly-digest.yml`(id 291903671,老身份,文件名不变),更名为 `Daily & Weekly Agents Radar`,用 `github.event.schedule` 区分跑日报还是周报。
   - 手动触发(workflow_dispatch, job=daily)**成功**:`Run daily digest` success、`Run weekly rollup` skipped,产出了当天日报。**证明脚本与分流逻辑正确。**
   - 但**定时仍未触发**:并入后,日报 cron 应在 2026-06-17 23:17 UTC 触发,到 2026-06-18 11:00(北京)仍无 `event=schedule` 记录。

## ~~当前结论(2026-06-18 复核 + 决定性证据)~~ 【已被 06-22 实测推翻,仅存档】

> ⚠️ 本节是 06-18 当天的推断,**已被 06-19~06-22 实测推翻**(独立调度器每天都触发,只是延迟 ~12h,详见顶部「最终结论」)。本节"fork 事件被丢弃"的判断不成立,保留仅为记录排查心路。

**问题不在脚本、不在 cron 表达式、不在 workflow 文件名、也不在任何一次具体改动,而在于:这个仓库是 fork,GitHub 对 fork 的 `schedule` 定时触发本身就不保证执行。本 fork 从 2026-06-15 之后,GitHub 实际一次定时都没再跑过。**

### 决定性铁证:fork 与 upstream 的 schedule 历史对照(2026-06-18 复核新增)

用 `gh api .../actions/runs?event=schedule` 同时查 fork 和上游原仓库:

| 仓库 | schedule 触发情况 |
|---|---|
| **upstream `duanyytop/agents-radar`(非 fork)** | **天天准点**:06-06~06-18 每天 cron `0 0 * * *` → 实际 00:2x–00:3x UTC,漂移仅几十秒~几分钟(GitHub 正常排队)。周报也准点。这是健康的定时。 |
| **fork `ivo-eu/agents-radar`(本仓库)** | 历史**总共只有 3 次**:06-13 10:15、06-14 11:10、06-15 18:19 UTC。**06-15 之后归零。** |

这一对照把结论从"强烈符合"升级为**确证**:同一套 workflow 文件、同样的 cron,upstream 准点天天跑,fork 几乎不跑。唯一变量就是 **fork 这个身份**。

补充:为什么"照抄上游也不行"。决定调度待遇的不是 workflow 文件内容,是仓库身份。upstream 是 839 星、11 watcher 的热门**独立**仓库,GitHub 给它正常 cron 调度;本 fork 是 **0 星 0 watcher 0 fork** 的 fork,GitHub 把它归入低优先级,scheduled 事件被静默丢弃。本 fork 的 workflow `state` 仍是 `active`(不是 `disabled_inactivity`),说明不是"被禁用",而是"事件不派发"。文件抄得再像也改不了仓库身份这个变量。

## 已落地的解决方案(2026-06-18)

采用"独立 GitHub 仓库调度器"——绕开 fork 的 cron 限流:

- 新建独立(非 fork)私有仓 **`ivo-eu/agents-radar-cron`**(`fork=false`,默认分支 `main`)。
- 其中 `.github/workflows/dispatch-daily-digest.yml`:cron `17 23 * * *`(= 次日 07:17 北京),用 secret `DISPATCH_TOKEN` 通过 REST `POST .../weekly-digest.yml/dispatches`(`ref=master, inputs.job=daily`)远程触发本 fork 的日报。
- `workflow_dispatch` 是显式事件,**不受 fork cron 限流**;调度器仓是独立仓,**它的 cron 可靠**;全程在 GitHub 服务器跑,**与本机是否开机无关**。
- 已端到端验证:手动触发调度器 → 成功在 fork 新建日报 run(`27735225874`)。
- token:复用本机 gh 的 OAuth token(scope `repo,workflow`),存入调度器仓 secret `DISPATCH_TOKEN`。**注意这是大权限 token,若日后收紧应换成仅 `actions:write` 且仅限 agents-radar 的 fine-grained PAT。**

验证调度器是否生效:
```bash
gh run list -R ivo-eu/agents-radar-cron -L 5
gh api "repos/ivo-eu/agents-radar/actions/workflows/weekly-digest.yml/runs?per_page=5" \
  --jq '.workflow_runs[] | "\(.created_at)\t\(.event)\t\(.status)/\(.conclusion)"'
```

### 对本文档原结论的两处纠正

1. **那 3 次不是"漂移 10+ 小时"。** 原文推断为排队延迟是错的。对照 upstream 的准点表现可知:fork 的 cron **从来没被准点调度过**;10:15 / 11:10 / 18:19 这种时间是 GitHub 对 fork 的"随缘补偿式"偶发唤醒,不是 cron 准点触发。
2. **"周报正常"不是单纯错觉,而是"fork 上没有任何 cron 可靠"。** 日报、周报一视同仁地不可靠;06-15 之前看到的"成功日报"里,相当一部分是 push 顺带带起或手动触发跑出来的,不是定时。用户以为定时在工作,其实看到的是**上游原仓库**天天准点产出的日报。

### 为什么"改好多次都没好"

每次修复(06-15 改文件名、06-17 把日报 cron 并入周报文件)都假设问题在配置层。但根因在 fork 身份上,配置怎么改都不会让 fork 的 schedule 变可靠——这正是反复修都无效的原因。

GitHub 已知行为佐证:fork 的 scheduled workflow 默认可能被禁用;60 天无活动会自动禁用 cron(本仓库一直活跃,不适用);**即便启用,fork 上 cron 触发也常被静默丢弃**——本仓库正是最后这种情况。

## 关键事实速查(给下一个排查者)

| 项 | 值 |
|---|---|
| fork? | 是,parent=`duanyytop/agents-radar` |
| 默认分支 | `master` |
| Actions enabled | true,allowed_actions=all |
| 老 workflow(可偶发触发) | Weekly=291903671, Monthly=291903670, CI=291903668(created 2026-06-09) |
| 新 workflow(从未 schedule 触发) | Daily=296046620(created 2026-06-15,现已降级为手动备份) |
| 承载日报+周报定时的文件 | `.github/workflows/weekly-digest.yml`(name: Daily & Weekly Agents Radar) |
| 手动补跑日报 | `gh workflow run weekly-digest.yml -R ivo-eu/agents-radar -f job=daily` |
| 手动补跑周报 | `gh workflow run weekly-digest.yml -R ivo-eu/agents-radar -f job=weekly` |
| 查 schedule 触发历史 | `gh api "repos/ivo-eu/agents-radar/actions/runs?event=schedule&per_page=20" --jq '.workflow_runs[]|"\(.created_at) \(.name) \(.conclusion)"'` |

## 下一步建议(按 2026-06-18 复核后的有效性重排)

> 重要:原"第 1 条 网页端启用"对本仓库**基本无效**。本仓库 6 月一直在 push,不存在 60 天禁用;06-15 后 schedule 归零说明问题不是"没启用",而是 fork 的 cron 触发被 GitHub 静默丢弃。点启用按钮(若存在)解决不了。真正有效的是下面 A / B 两条二选一。

### A. 外部触发(推荐,改动小、最稳)
彻底不依赖 fork 的 GitHub schedule,用一个**可靠的外部定时器**去调 `workflow_dispatch`:
- 任意常开机器的 crontab,每天定时跑:
  `gh workflow run weekly-digest.yml -R ivo-eu/agents-radar -f job=daily`
  (等价 REST:`POST /repos/ivo-eu/agents-radar/actions/workflows/291903671/dispatches`,body `{"ref":"master","inputs":{"job":"daily"}}`)
- 或 cron-job.org / Cloudflare Worker / GitHub Actions 在**另一个非 fork 仓库**里定时发 dispatch。
- 本机已有 **Radar Bridge** 频繁往该仓库 push(favorites sync),可在那条链路挂一个"每天首次同步时顺带触发日报"的钩子。
- ⚠️ `gh` 默认仓库会解析到 **upstream `duanyytop`**(会 403 Must have admin rights)。**务必带 `-R ivo-eu/agents-radar`**。

### B. detach fork(根治,改动大)
把仓库从 fork 转为独立仓库,scheduled workflow 即不再受 fork 限制。联系 GitHub Support 解除 fork 关系,或新建独立仓库迁移。代价:失去与上游 fork 关联(可改用 remote upstream 手动同步)。

### C. 保底:半自动
保留 `Daily & Weekly` 的手动触发入口,由 A 的外部定时器或人工每天触发一次。脚本本身完全正常(已多次手动验证产出日报)。

### 不再建议
- 继续改 workflow 文件名 / cron / name 字段——已证明在 fork 上无效,且改 name/文件名会注销旧调度。

## 已经确认是好的、不要再动的部分

- 脚本与分流逻辑正确(手动触发能产出日报/周报)。
- `weekly-digest.yml` 的 `github.event.schedule` 分流(周报 cron `0 22 * * 0` 跑 weekly,其余跑 daily)正确。
- **不要再改 workflow 文件名**——会注销旧 workflow、注册新 workflow,在 fork 上更触发不了。
- 周报/月报已迁到 `digests/weekly/`、`digests/monthly/` 独立目录,前端三分区、旧链接兼容均已验证通过(与定时问题无关)。
- ⚠️ **`gh` 默认仓库坑**:本地 `gh repo view` 解析到 upstream `duanyytop/agents-radar`。任何 `gh workflow run`、`gh api .../dispatches` 不带 `-R ivo-eu/agents-radar` 都会打到上游并报 `403 Must have admin rights`。手动补跑/外部触发务必显式指定 `-R ivo-eu/agents-radar`。

## 元信息

- 会话 ID:`0c5808a2-3a9d-4317-9879-6780711a3da5`
- 文档生成:2026-06-18(北京约 11:00 / 03:00 UTC)
- **2026-06-22 复核更新**:连续 4 天实测后,推翻"fork 事件被丢弃"旧结论,确认真因为
  "低优先级仓被 GitHub 统一延迟 ~12h",新增顶部「最终结论」节。用户决定接受傍晚出报。
- 工具:Claude Code,模型 claude-opus-4-8
